{
    "name": "${project.artifactId}",
    "version": "${project.version}",
    "description": "${project.description}",
    "homepage": "${project.url}",
    "author": {
        "name": "${author.name}",
        "email": "${author.email}",
        "email": "${author.homepage}"
    },
    "license": "${project.license}",
    "main": "combridge.js"
}
